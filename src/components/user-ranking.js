import React, {useState, useEffect} from "react"

const UserRanking = ({id, name, userName, points, weight}) => {

  return (
    <div>
      {/* implement me */}
    </div>
  )
};

export default UserRanking;
